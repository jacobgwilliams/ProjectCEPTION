<h2> Welcome to your dashboard, <%=@user.name %>! </h2>

<% if !round_two? %>
  <%= render "round_1_show" %>
<% else %>
  <%= render "round_2_show" %>
<% end %>

<%= link_to "Generate New Access Keys", new_secret_key_path %>
